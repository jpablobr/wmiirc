status:
  mouse:
    script: |
      def enabled
       `xinput list-props "ImPS/2 ALPS GlidePoint" | awk '/Device Enabled/ {print $NF}'`.match /1/
      end
    label: |
      color = enabled ? 'error' : 'success'

      [color, "\u270D"]
